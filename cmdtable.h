/* cmdtable.h
	This file generated by makecmds. It contains the
	data structure for the main command jumptable and a list
	of the command name values. DON'T EDIT THIS FILE
*/

#define MAXCMDKEY 8
#define KEY_ESC 0x1b
#define NUMCMDS 129

enum { HELP_NOT=0, HELPSECT_BLOCK, HELPSECT_CURSOR,
	HELPSECT_DELETE, HELPSECT_DISPLAY, HELPSECT_EDIT,
	HELPSECT_FILE, HELPSECT_FIND, HELPSECT_MACROS, HELPSECT_MENU,
	HELPSECT_MISC, HELPSECT_SPELL, HELPSECT__CURSOR, HELP_TOTAL };

enum { FIRSTVEDCOMMAND=0x1000, AUTONUMBER_SET, BACKSPACE,
	BLOCK_CMD, BLOCK_COPY, BLOCK_CUT, BLOCK_DELETE, BLOCK_DELMARKS,
	BLOCK_DOUBLE, BLOCK_END, BLOCK_GET, BLOCK_MOVE, BLOCK_PASTE,
	BLOCK_SAVE, BLOCK_SORT, BLOCK_START, BLOCK_TO_BUFFER,
	BLOCK_UNFORMAT, BLOCK_WORDWRAP, BLOCK_YGET, BUFFER_INFO, CASER,
	CHANGE_WORD, COPY_LINE, CREATE_RC, CURSOR_DOWN, CURSOR_ENDPP,
	CURSOR_ENDWORD, CURSOR_FILEEND, CURSOR_FILESTART,
	CURSOR_LEFT, CURSOR_LINEEND, CURSOR_LINESTART, CURSOR_MATCH,
	CURSOR_PAGEBOTTOM, CURSOR_PAGEDOWN, CURSOR_PAGETOP,
	CURSOR_PAGEUP, CURSOR_RIGHT, CURSOR_STARTNWORD,
	CURSOR_STARTPP, CURSOR_STARTPWORD, CURSOR_STARTWORD,
	CURSOR_UP, DELETE, DELETE_ENDWORD, DELETE_LINE, DELETE_TOEOL,
	DELETE_TOEOS, DELETE_WORD, DOSHELL, FILE_APPEND, FILE_NEW, FIND,
	FIND_BACK, FIND_NEXT, FIND_PRIOR, GET_CLINE, GET_LINE,
	HELP_BLOCK, HELP_CURSOR, HELP_DELETE, HELP_DISPLAY, HELP_EDIT,
	HELP_FILE, HELP_MACROS, HELP_MENUS, HELP_MISC, HELP_SEARCH,
	HELP_SPELL, HEXINPUT, INSERT, INSERT_TAB, JUMP, KEYBOARD_ABORT,
	LOAD_RC, MACRO_FILENAME, MACRO_FILE_BASE, MACRO_FILE_EXT,
	MACRO_LEARN, MACRO_NUMBER, MACRO_START, MACRO_TIME1,
	MACRO_TIME2, MARK_BLOCK_END, MARK_BLOCK_START, MARK_JUMP,
	MARK_POSITION, MENU0_ENABLE, MENU1_ENABLE, MENU2_ENABLE,
	MENU3_ENABLE, MENU4_ENABLE, MENU5_ENABLE, MENU6_ENABLE, MOUSE,
	MOVE_WINDOW, QUIT, QUIT_ALL, REPEAT_KEY, REPLACE, REPLACE_AGAIN,
	RESIZE_WINDOW, SCROLL_LEFT, SCROLL_RIGHT, SET_KEYBINDINGS,
	SET_RC_LINE, SET_TABWIDTH, SET_WILDCARD, SPELLER_BLOCK,
	SPELLER_FILE, SPELLER_SAVE_NEW_WORDS, SPELLER_WORD,
	TERMINAL_REFRESH, TOGGLE_AUTOINDENT, TOGGLE_AUTONUM,
	TOGGLE_CASEMATCH, TOGGLE_CMODE, TOGGLE_CRDISPLAY,
	TOGGLE_OVERWRITE, TOGGLE_WORDWRAP, TRANSPOSE, UNDELETE,
	WINDOWS_FILE, WINDOWS_NEW, WINDOWS_NEXT, WINDOWS_PRIOR,
	WINDOWS_RAISE, WINDOWS_SHOWALL, XSAVE_BUFFER,
	 LASTVEDCOMMAND };

typedef struct
{
	u_char	*cmdname;		/* name of command (uppercase of func name) */
	void	(*cmdfunc)(void);	/* pointer to the function */
	u_char	*comment;		/* descriptive comment */
	u_char	*user_comment;		/* user supplied comment */
	u_char	*keyname;		/* name on keyboard (for help) */
	u_char	*user_keyname;		/* user replaced keyboard name */
	KEY	keys[MAXCMDKEY];	/* command seq. keys (ESC> <A>...*/
	KEY	cmdvalue;		/* internal value of command */
	int	menu;			/* help menu to display this cmd */
	u_char	noedit;			/* set==don't permit in view mode */
	u_char	kcount;			/* number of keys in command seq. */
}CMDTABLE;


extern CMDTABLE cmdtable[];

typedef struct
{
	KEY	cmdvalue;		/* the command, again */
	KEY	keys[MAXCMDKEY];	/* more possible key sequences */
	u_char	kcount;			/* key count for alias sequence */
}ALIAS_CMDTABLE;


extern ALIAS_CMDTABLE alias_cmdtable[];

#define MAXALIAS 50	/* controls size of alias_cmdtable */

/* EOF */

